import { describe, expect, it } from 'bun:test';
import '../../src/helpers/types';

describe('types module', () => {
  it('loads without runtime errors', () => {
    expect(true).toBe(true);
  });
});
