services:
  # PostgreSQL Database
  postgres:
    image: postgres:15
    container_name: postgres-prod
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: cqrs_demo_prod
    ports:
      - "5432:5432"
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    networks:
      - app-network

  # User Service
  service-user:
    image: bun-hono-redis-cqrs/service-user:prod
    container_name: service-user-prod
    environment:
      NODE_ENV: prod
      DB_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/cqrs_demo_prod
      JWT_SECRET: ${JWT_SECRET}
      USER_SERVICE_PORT: 3101
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "3101:3101"
    depends_on:
      - postgres
      - redis
    networks:
      - app-network

  # Product Service
  service-product:
    image: bun-hono-redis-cqrs/service-product:prod
    container_name: service-product-prod
    environment:
      NODE_ENV: prod
      DB_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/cqrs_demo_prod
      PRODUCT_SERVICE_PORT: 3102
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "3102:3102"
    depends_on:
      - postgres
      - redis
    networks:
      - app-network

  # Auth Service
  service-auth:
    image: bun-hono-redis-cqrs/service-auth:prod
    container_name: service-auth-prod
    environment:
      NODE_ENV: prod
      JWT_SECRET: ${JWT_SECRET}
      AUTH_SERVICE_PORT: 3100
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "3100:3100"
    networks:
      - app-network

  redis:
    image: redis:7.2-alpine
    container_name: redis-prod
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_prod_data:/data
    networks:
      - app-network

volumes:
  postgres_prod_data:
  redis_prod_data:

networks:
  app-network:
    driver: bridge
