{
  "database": {
    "url": "postgresql://postgres:postgres@prod-db:5432/cqrs_demo_prod?schema=public"
  },
  "services": {
    "userService": {
      "port": 3101
    },
    "productService": {
      "port": 3102
    },
    "authService": {
      "port": "${PORT}"
    }
  },
  "redis": {
    "host": "${REDIS_HOST}",
    "port": "${REDIS_PORT}",
    "password": "${REDIS_PASSWORD}",
    "db": "${REDIS_DB}",
    "keyPrefix": "auth:",
    "streams": {
      "maxLen": 100000,
      "blockMs": 5000
    }
  },
  "logging": {
    "level": "warn",
    "pretty": false
  },
  "metrics": {
    "enabled": true,
    "collectDefaultMetrics": true,
    "prefix": "cqrs_prod_",
    "buckets": [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1, 5, 10]
  },
  "security": {
    "encryptionEnabled": true,
    "keyRotationInterval": 2592000000,
    "auditLogging": true
  },
  "features": {
    "hotReload": false,
    "autoMigrate": false,
    "debugMode": false,
    "mockExternalServices": false
  }
}
